# 「取走黄金」Unity 实施计划

## 目标

在 `EnvironmentScene` 的 `PlayerSpawn`（截图红框区域）部署一个完全独立的 3D 「取走黄金」玩法。旧的 `Stacking House VR` 作为场景内容保留但停用，新玩法使用原悟空环境、金砖主视觉和中英文可切换 UI。

## 自动执行节点

- [x] 1. 确认当前场景、Unity 版本和红框锚点。
- [x] 2. 优先检索 `ange-embed`，引入金砖 GLB 及来源记录；游戏使用自定义倒角金砖网格并复用该资产材质。
- [x] 3. 编写独立运行时：横移、落块、交集裁切、残块坠落、Perfect、连击、黄金层、钻石、星级、救援和结算。
- [x] 4. 编写独立场景安装器：以 `PlayerSpawn` 定位平台、相机、HUD、输入目标和物理基础设施。
- [x] 5. 让 Unity 完成首轮脚本编译并执行 `Tools > Stackline Classic > Create or Refresh in Current Scene`。
- [x] 6. 在 Play Mode 验证首轮菜单、首次落块、裁切、失败、救援和 Console 日志。
- [x] 7. 完成默认经典模式：正交取景、常驻漂浮粒子、Perfect 白色扩散方框和每 10 层黄金奖励。
- [x] 8. 完成 HUD：中央大分数、右上资源纵排、失败保塔覆盖层，并采用黑玻璃、细金边和金色强调的移动端视觉。
- [x] 9. 补齐设置、挑战、黄金收藏、本地排行榜、双倍奖励、生命商店、中英文切换及本地持久化。
- [x] 10. 在 Unity 中刷新、重建并保存场景；回归菜单、落块、裁切、Perfect、失败/救援、结算和重开主路径。
- [x] 11. 完成最终 Unity 脚本编译、Game 视图、中英文持久化、金砖材质与已保存场景验证。

## 主题升级记录

- [x] 主题文案改为「取走黄金」 / `TAKE THE GOLD`，主操作改为「点击取走黄金」 / `TAP TO TAKE GOLD`。
- [x] 移除程序化纯色背景，恢复原悟空场景、天空盒和环境构图。
- [x] 所有塔块、移动块、裁切块和菜单预览均使用上小下大、四角倒角的金砖外形，逻辑碰撞仍使用稳定的 `BoxCollider`。
- [x] 运行时克隆并调校 GLB 材质，不修改 `ange-embed` 原始资产；降低过曝与专属灯光强度。
- [x] 中文与 English 覆盖主菜单、设置、挑战、黄金收藏、排行榜、奖励、生命、救援和提示，切换结果独立持久化。
- [x] 二级页使用可滚动黑玻璃容器，已修复内容裁切、奖励图标本地化和关闭后无法再开启声音/震动的问题。
- [x] 移除与当前 `InputSystemUIInputModule` 不匹配的 XR UI Raycaster，消除退出 Play Mode 时的 XRI 清理异常；独立 XR 落块交互仍保留。

## 已知工程提示

- PICO Interaction Demo 示例包每次进入 Play Mode 会打开 Project Validation，提示示例缺少 TMP Essentials；与 Stackline 运行时无关。
- 原场景会报 Terrain 相邻高度图分辨率不一致及阴影图降级警告；本轮未改动地形与原场景灯光。

## 验收标准

- 场景中只有独立的 `Stackline Classic Game` 参与运行，旧玩法根节点处于 inactive。
- 鼠标、触屏、空格/回车都能落块；XR 选择目标可选地触发同一落块事件。
- 重叠不足会坠落并进入救援/结算流程，重叠会裁切并生成下一层；连续精准落点、黄金层和高度奖励可见。
- 相机能从平台基础开始取景，塔身升高后平滑上移，UI 不遮挡主要塔身。
- 默认经典模式的新档资源与参考一致：钻石 2、生命 0、星星 0；每 10 层只增加 1 钻石。
- 菜单具有设置、挑战、皮肤、排行榜、去广告和生命入口；二级页面可操作并保存状态。
- 游戏中不显示调试式英文教学横条，Perfect 使用贴合顶层尺寸的白色轮廓反馈。
- 失败后保留本局塔作为背景，显示最终/最佳分数并可直接点击开始下一局。
